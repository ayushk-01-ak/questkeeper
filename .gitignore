# Virtual environment — never commit this
venv/

# Python cache files
__pycache__/
*.pyc
*.pyo

# VS Code settings
.vscode/

# Database files (we'll handle these carefully later)
*.db

# Environment variables (API keys etc — never commit these)
.env

# OS junk
.DS_Store
Thumbs.db

# Database — contains player data, not source code
questkeeper.db